/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type { GetUserInfoQueryResponse, GetUserInfo401, GetUserInfo500 } from '../types/GetUserInfo.ts';
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query';
import { getUserInfo } from '../clients/getUserInfo.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const getUserInfoQueryKey = () => [{ url: '/users/me' }] as const;

export type GetUserInfoQueryKey = ReturnType<typeof getUserInfoQueryKey>;

export function getUserInfoQueryOptions(config: Partial<RequestConfig> & { client?: Client } = {}) {
	const queryKey = getUserInfoQueryKey();
	return queryOptions<
		GetUserInfoQueryResponse,
		ResponseErrorConfig<GetUserInfo401 | GetUserInfo500>,
		GetUserInfoQueryResponse,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			return getUserInfo({ ...config, signal: config.signal ?? signal });
		},
	});
}

/**
 * @description Use to show the logged-in user's profile in the UI or to check permissions and roles for the current session.
 * @summary Get current user
 * {@link /users/me}
 */
export function useGetUserInfo<
	TData = GetUserInfoQueryResponse,
	TQueryData = GetUserInfoQueryResponse,
	TQueryKey extends QueryKey = GetUserInfoQueryKey,
>(
	options: {
		query?: Partial<
			QueryObserverOptions<GetUserInfoQueryResponse, ResponseErrorConfig<GetUserInfo401 | GetUserInfo500>, TData, TQueryData, TQueryKey>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getUserInfoQueryKey();

	const query = useQuery(
		{
			...getUserInfoQueryOptions(config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<GetUserInfo401 | GetUserInfo500>> & { queryKey: TQueryKey };

	query.queryKey = queryKey as TQueryKey;

	return query;
}
