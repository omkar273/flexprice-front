/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
	GetEntitlementQueryResponse,
	GetEntitlementPathParams,
	GetEntitlement400,
	GetEntitlement500,
} from '../types/GetEntitlement.ts';
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query';
import { getEntitlement } from '../clients/getEntitlement.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const getEntitlementQueryKey = (id: GetEntitlementPathParams['id'] | undefined) =>
	[{ url: '/entitlements/:id', params: { id: id } }] as const;

export type GetEntitlementQueryKey = ReturnType<typeof getEntitlementQueryKey>;

export function getEntitlementQueryOptions(
	id: GetEntitlementPathParams['id'] | undefined,
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const queryKey = getEntitlementQueryKey(id);
	return queryOptions<
		GetEntitlementQueryResponse,
		ResponseErrorConfig<GetEntitlement400 | GetEntitlement500>,
		GetEntitlementQueryResponse,
		typeof queryKey
	>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return getEntitlement(id!, { ...config, signal: config.signal ?? signal });
		},
	});
}

/**
 * @description Use when you need to load a single entitlement (e.g. to display or edit a feature limit).
 * @summary Get entitlement
 * {@link /entitlements/:id}
 */
export function useGetEntitlement<
	TData = GetEntitlementQueryResponse,
	TQueryData = GetEntitlementQueryResponse,
	TQueryKey extends QueryKey = GetEntitlementQueryKey,
>(
	id: GetEntitlementPathParams['id'] | undefined,
	options: {
		query?: Partial<
			QueryObserverOptions<
				GetEntitlementQueryResponse,
				ResponseErrorConfig<GetEntitlement400 | GetEntitlement500>,
				TData,
				TQueryData,
				TQueryKey
			>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getEntitlementQueryKey(id);

	const query = useQuery(
		{
			...getEntitlementQueryOptions(id, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<GetEntitlement400 | GetEntitlement500>> & { queryKey: TQueryKey };

	query.queryKey = queryKey as TQueryKey;

	return query;
}
