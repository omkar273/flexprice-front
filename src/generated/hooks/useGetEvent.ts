/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type { GetEventQueryResponse, GetEventPathParams, GetEvent404, GetEvent500 } from '../types/GetEvent.ts';
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query';
import { getEvent } from '../clients/getEvent.ts';
import { queryOptions, useQuery } from '@tanstack/react-query';

export const getEventQueryKey = (id: GetEventPathParams['id'] | undefined) => [{ url: '/events/:id', params: { id: id } }] as const;

export type GetEventQueryKey = ReturnType<typeof getEventQueryKey>;

export function getEventQueryOptions(id: GetEventPathParams['id'] | undefined, config: Partial<RequestConfig> & { client?: Client } = {}) {
	const queryKey = getEventQueryKey(id);
	return queryOptions<GetEventQueryResponse, ResponseErrorConfig<GetEvent404 | GetEvent500>, GetEventQueryResponse, typeof queryKey>({
		enabled: !!id,
		queryKey,
		queryFn: async ({ signal }) => {
			return getEvent(id!, { ...config, signal: config.signal ?? signal });
		},
	});
}

/**
 * @description Use when debugging a specific event (e.g. why it failed or how it was aggregated). Includes processing status and debug info.
 * @summary Get event
 * {@link /events/:id}
 */
export function useGetEvent<
	TData = GetEventQueryResponse,
	TQueryData = GetEventQueryResponse,
	TQueryKey extends QueryKey = GetEventQueryKey,
>(
	id: GetEventPathParams['id'] | undefined,
	options: {
		query?: Partial<
			QueryObserverOptions<GetEventQueryResponse, ResponseErrorConfig<GetEvent404 | GetEvent500>, TData, TQueryData, TQueryKey>
		> & { client?: QueryClient };
		client?: Partial<RequestConfig> & { client?: Client };
	} = {},
) {
	const { query: queryConfig = {}, client: config = {} } = options ?? {};
	const { client: queryClient, ...resolvedOptions } = queryConfig;
	const queryKey = resolvedOptions?.queryKey ?? getEventQueryKey(id);

	const query = useQuery(
		{
			...getEventQueryOptions(id, config),
			...resolvedOptions,
			queryKey,
		} as unknown as QueryObserverOptions,
		queryClient,
	) as UseQueryResult<TData, ResponseErrorConfig<GetEvent404 | GetEvent500>> & { queryKey: TQueryKey };

	query.queryKey = queryKey as TQueryKey;

	return query;
}
