{
	"required": ["line_items"],
	"type": "object",
	"properties": {
		"line_items": {
			"minItems": 1,
			"type": "array",
			"items": {
				"required": ["id", "quantity"],
				"type": "object",
				"properties": {
					"effective_date": {
						"type": "string",
						"description": "EffectiveDate is when the quantity change takes effect.\nIf omitted, the change is effective immediately (now).",
						"format": "date-time"
					},
					"id": { "type": "string" },
					"quantity": { "type": "string" }
				},
				"x-readme-ref-name": "LineItemQuantityChange"
			}
		}
	},
	"x-readme-ref-name": "SubModifyQuantityChangeRequest"
}
