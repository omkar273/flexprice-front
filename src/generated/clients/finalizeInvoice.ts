/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
	FinalizeInvoiceMutationResponse,
	FinalizeInvoicePathParams,
	FinalizeInvoice400,
	FinalizeInvoice500,
} from '../types/FinalizeInvoice.ts';

function getFinalizeInvoiceUrl(id: FinalizeInvoicePathParams['id']) {
	const res = { method: 'POST', url: `/invoices/${id}/finalize` as const };
	return res;
}

/**
 * @description Use when locking an invoice for payment (e.g. after review). Once finalized, line items are locked; invoice can be paid or voided.
 * @summary Finalize invoice
 * {@link /invoices/:id/finalize}
 */
export async function finalizeInvoice(id: FinalizeInvoicePathParams['id'], config: Partial<RequestConfig> & { client?: Client } = {}) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<FinalizeInvoiceMutationResponse, ResponseErrorConfig<FinalizeInvoice400 | FinalizeInvoice500>, unknown>({
		method: 'POST',
		url: getFinalizeInvoiceUrl(id).url.toString(),
		...requestConfig,
	});
	return res.data;
}
