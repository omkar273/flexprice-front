/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
	GetInvoiceQueryResponse,
	GetInvoicePathParams,
	GetInvoiceQueryParams,
	GetInvoice404,
	GetInvoice500,
} from '../types/GetInvoice.ts';

function getGetInvoiceUrl(id: GetInvoicePathParams['id']) {
	const res = { method: 'GET', url: `/invoices/${id}` as const };
	return res;
}

/**
 * @description Use when loading an invoice for display or editing (e.g. portal or reconciliation). Supports group_by for usage breakdown and force_runtime_recalculation.
 * @summary Get invoice
 * {@link /invoices/:id}
 */
export async function getInvoice(
	id: GetInvoicePathParams['id'],
	params?: GetInvoiceQueryParams,
	config: Partial<RequestConfig> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<GetInvoiceQueryResponse, ResponseErrorConfig<GetInvoice404 | GetInvoice500>, unknown>({
		method: 'GET',
		url: getGetInvoiceUrl(id).url.toString(),
		params,
		...requestConfig,
	});
	return res.data;
}
