/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type { GetPriceQueryResponse, GetPricePathParams, GetPrice400, GetPrice500 } from '../types/GetPrice.ts';

function getGetPriceUrl(id: GetPricePathParams['id']) {
	const res = { method: 'GET', url: `/prices/${id}` as const };
	return res;
}

/**
 * @description Use when you need to load a single price (e.g. for display or editing). Response includes expanded meter and price unit when applicable.
 * @summary Get price
 * {@link /prices/:id}
 */
export async function getPrice(id: GetPricePathParams['id'], config: Partial<RequestConfig> & { client?: Client } = {}) {
	const { client: request = fetch, ...requestConfig } = config;

	const res = await request<GetPriceQueryResponse, ResponseErrorConfig<GetPrice400 | GetPrice500>, unknown>({
		method: 'GET',
		url: getGetPriceUrl(id).url.toString(),
		...requestConfig,
	});
	return res.data;
}
