/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from '../../api/kubbClient';
import type { Client, RequestConfig, ResponseErrorConfig } from '../../api/kubbClient';
import type {
	QueryCustomerMutationRequest,
	QueryCustomerMutationResponse,
	QueryCustomer400,
	QueryCustomer500,
} from '../types/QueryCustomer.ts';

function getQueryCustomerUrl() {
	const res = { method: 'POST', url: `/customers/search` as const };
	return res;
}

/**
 * @description Use when listing or searching customers (e.g. admin CRM or reporting). Returns a paginated list; supports filtering and sorting.
 * @summary Query customers
 * {@link /customers/search}
 */
export async function queryCustomer(
	data: QueryCustomerMutationRequest,
	config: Partial<RequestConfig<QueryCustomerMutationRequest>> & { client?: Client } = {},
) {
	const { client: request = fetch, ...requestConfig } = config;

	const requestData = data;

	const res = await request<
		QueryCustomerMutationResponse,
		ResponseErrorConfig<QueryCustomer400 | QueryCustomer500>,
		QueryCustomerMutationRequest
	>({ method: 'POST', url: getQueryCustomerUrl().url.toString(), data: requestData, ...requestConfig });
	return res.data;
}
